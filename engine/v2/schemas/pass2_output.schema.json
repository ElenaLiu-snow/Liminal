{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://liminal.local/schemas/v2/pass2_output.schema.json",
  "title": "Liminal v2 Pass 2 integration output",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "session_id",
    "card_id",
    "orientation",
    "pass1_sha256",
    "user_question",
    "question_shift",
    "question_shift_note",
    "central_axis",
    "pattern_inheritance",
    "process_recap",
    "reality_evidence",
    "situated_traditional_reading",
    "compensation_bridge",
    "bounded_direction",
    "practical_translation",
    "epistemic_limits",
    "user_display"
  ],
  "properties": {
    "schema_version": {"const": "2.0-alpha.1"},
    "session_id": {"type": "string", "minLength": 1},
    "card_id": {"type": "string", "minLength": 1},
    "orientation": {"enum": ["upright", "reversed"]},
    "pass1_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "user_question": {"type": "string", "minLength": 1},
    "question_shift": {
      "enum": ["unchanged", "clarified", "shifted_focus", "different_question"]
    },
    "question_shift_note": {"type": ["string", "null"]},
    "central_axis": {"type": "string", "minLength": 1},
    "pattern_inheritance": {
      "type": "array",
      "items": {"$ref": "#/$defs/patternInheritance"}
    },
    "process_recap": {"type": "string", "minLength": 1, "maxLength": 500},
    "reality_evidence": {
      "type": "array",
      "items": {"$ref": "#/$defs/realityEvidence"}
    },
    "situated_traditional_reading": {
      "$ref": "traditional_reading.schema.json#/$defs/situatedOutput"
    },
    "compensation_bridge": {"$ref": "#/$defs/compensationBridge"},
    "bounded_direction": {"$ref": "#/$defs/boundedDirection"},
    "practical_translation": {"$ref": "#/$defs/practicalTranslation"},
    "epistemic_limits": {
      "type": "object",
      "additionalProperties": false,
      "required": ["unsupported_inferences", "limitations"],
      "properties": {
        "unsupported_inferences": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "enum": [
              "stable_trait",
              "developmental_origin",
              "childhood_experience",
              "trauma_source",
              "clinical_diagnosis"
            ]
          }
        },
        "limitations": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        }
      }
    },
    "user_display": {
      "type": "object",
      "additionalProperties": false,
      "required": ["complete_reading", "takeaway_question"],
      "properties": {
        "complete_reading": {"type": "string", "minLength": 1},
        "takeaway_question": {"type": "string", "minLength": 1}
      }
    }
  },
  "$defs": {
    "patternInheritance": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "pattern_id",
        "status",
        "match_basis",
        "rationale",
        "process_step_mappings",
        "adaptive_value_in_context",
        "current_cost_in_context"
      ],
      "properties": {
        "pattern_id": {"type": "string", "minLength": 1},
        "status": {"enum": ["integrated", "held", "not_relevant"]},
        "match_basis": {
          "enum": [
            "process_recurrence",
            "theme_overlap_only",
            "insufficient_process_evidence",
            "no_material_match"
          ]
        },
        "rationale": {"type": "string", "minLength": 1},
        "process_step_mappings": {
          "type": "array",
          "uniqueItems": true,
          "items": {"$ref": "#/$defs/processStepMapping"}
        },
        "adaptive_value_in_context": {"type": ["string", "null"]},
        "current_cost_in_context": {"type": ["string", "null"]}
      }
    },
    "processStepMapping": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pass1_step", "question_manifestation", "reality_evidence_ids"],
      "properties": {
        "pass1_step": {"type": "string", "minLength": 1},
        "question_manifestation": {"type": "string", "minLength": 1},
        "reality_evidence_ids": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        }
      }
    },
    "realityEvidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "source", "quote", "role", "interpretation"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "source": {"enum": ["user_question", "question_shift_note"]},
        "quote": {"type": "string", "minLength": 1},
        "role": {
          "enum": ["supporting", "disconfirming", "constraint", "action_already_taken"]
        },
        "interpretation": {"type": "string", "minLength": 1}
      }
    },
    "compensationBridge": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "inherited_process_limit",
        "card_counterweight",
        "revised_decision_criterion"
      ],
      "properties": {
        "inherited_process_limit": {"type": "string", "minLength": 1},
        "card_counterweight": {"type": "string", "minLength": 1},
        "revised_decision_criterion": {"type": "string", "minLength": 1}
      }
    },
    "boundedDirection": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "answer",
        "uncertainty_boundary",
        "evidence_pattern_ids",
        "reality_evidence_ids"
      ],
      "properties": {
        "answer": {"type": "string", "minLength": 1},
        "uncertainty_boundary": {"type": "string", "minLength": 1},
        "evidence_pattern_ids": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        },
        "reality_evidence_ids": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        }
      }
    },
    "practicalTranslation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "mode",
        "redefined_success",
        "step_or_practice",
        "rationale",
        "evidence_pattern_ids",
        "reality_evidence_ids"
      ],
      "properties": {
        "mode": {"enum": ["action", "reflection", "none"]},
        "redefined_success": {"type": "string", "minLength": 1},
        "step_or_practice": {"type": ["string", "null"]},
        "rationale": {"type": "string", "minLength": 1},
        "evidence_pattern_ids": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        },
        "reality_evidence_ids": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        }
      }
    }
  }
}
