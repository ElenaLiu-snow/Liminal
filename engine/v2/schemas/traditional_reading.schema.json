{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://liminal.local/schemas/v2/traditional_reading.schema.json",
  "title": "Liminal v2 traditional tarot layer contracts",
  "$defs": {
    "canonicalReading": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "card_id",
        "orientation",
        "stable_meaning",
        "visual_symbols",
        "core_themes",
        "source_refs",
        "source_status"
      ],
      "properties": {
        "card_id": {"type": "string", "minLength": 1},
        "orientation": {"enum": ["upright", "reversed"]},
        "stable_meaning": {"type": "string", "minLength": 1},
        "visual_symbols": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        },
        "core_themes": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        },
        "source_refs": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "source_status": {
          "enum": ["provisional_v1_dataset", "waite_verified", "human_reviewed"]
        }
      }
    },
    "situatedOutput": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "card_id",
        "orientation",
        "question_relevant_card_structure",
        "orientation_mechanism",
        "tension_axes",
        "scope_boundary",
        "source_refs"
      ],
      "properties": {
        "card_id": {"type": "string", "minLength": 1},
        "orientation": {"enum": ["upright", "reversed"]},
        "question_relevant_card_structure": {"type": "string", "minLength": 1},
        "orientation_mechanism": {"type": "string", "minLength": 1},
        "tension_axes": {
          "type": "array",
          "minItems": 2,
          "items": {"type": "string", "minLength": 1}
        },
        "scope_boundary": {"type": "string", "minLength": 1},
        "source_refs": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        }
      }
    }
  }
}
