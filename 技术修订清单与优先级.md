# Liminal · 技术修订清单与优先级

> 日期：2026-07-28
> 目的：把本轮产品讨论转成可执行的技术工作，并明确依赖、优先级和验收条件。
> 原则：每个阶段单独实现、测试和验收；未通过当前阶段前，不提前叠加下一阶段。

---

## 优先级定义

| 优先级 | 含义 |
|---|---|
| **P0 · 阻断** | 当前契约不合法或相互矛盾，不修复就不能安全进入新架构 |
| **P1 · 核心** | 直接实现 Liminal 的差异化机制 |
| **P2 · 质量** | 决定解读是否可靠、可解释、可评测 |
| **P3 · 产品化** | 将已验证的引擎包装成完整用户体验 |

---

## P0 · 契约修复与 v1 基线冻结

**目标**：先让当前 v1 成为合法、可运行、可回归的基线。

### P0-1 修复 `analysis_schema.json`

- 删除第 165 行开始误拼接的旧版 schema
- 保留前半段 v1.1 三部分输出契约
- 确认文件可被标准 JSON parser 读取

**验收**：

- `python -m json.tool engine/analysis_schema.json` 通过
- schema 顶层只包含 `version`、`description`、`input_schema`、`analysis_layer_output`、`pipeline_notes`

### P0-2 收口 v1 架构口径

- canonical v1 口径：分析层三部分输出直接面向用户
- writing layer 是历史实验文件，v1.1 中处于 deferred 状态
- 删除的旧 schema 代表已废弃的“分析层不展示、只供 writing layer 使用”架构

**验收**：

- baseline 文档明确记录上述口径
- 合同测试检查 `pipeline_notes.mvp_output`

### P0-3 统一 delta 阈值

- canonical v1 阈值统一为 `delta >= 0.3`
- 主题空间分数仍只作为 v1 启发式辅助信号
- v2 不把该阈值当作心理测量结果

**验收**：

- schema、analysis prompt 与数据结构说明均使用 `>= 0.3`

### P0-4 冻结回归基线

- 记录 v1 schema、prompts 和三份 trial reading 的文件哈希
- 明确这些文件是后续比较基线，不代表 v2 目标质量
- 不移动、不覆盖现有用户试读文件

**验收**：

- `engine/baselines/v1/manifest.json` 存在并可解析
- manifest 中记录所有基线文件和 SHA-256

### P0-5 增加最小合同测试

- 验证 schema 合法且版本为 1.1
- 验证三部分输出结构存在
- 验证 78 张牌数据均为合法 JSON
- 验证牌数为 22 + 14 × 4
- 验证时间取向和关系方向的数值和

**验收**：

- 标准库 `unittest` 全部通过

---

## P1 · 象征转换关系模型

**目标**：把核心技术对象从“用户偏离标准牌义多少”升级为“用户如何重新组织象征材料”。

### 需要实现

- `symbolic_observations`
  - `noticed`
  - `omitted`
  - `added`
- `symbolic_transformations`
  - `convergence`
  - `omission`
  - `inversion`
  - `addition`
  - `emotional_recoding`
  - `agency_shift`
  - `identification_shift`
  - `temporal_shift`
- 每条转换包含：
  - 用户原话证据
  - 牌面/传统象征证据
  - 首选解释
  - 替代解释
  - 反对证据
  - 置信度
- `psychological_patterns` 把多个有共同证据的转换组织为过程，而不是给用户贴人格标签：
  - `sequence`：本次联想中的思考/注意顺序
  - `emotional_function`：该顺序调节或保护什么
  - `adaptive_value`：它在何种情况下有帮助
  - `current_cost`：它在本次会话中可能带来的限制
  - `scope`：单次会话固定为 `session_pattern`
  - 证据、替代解释、反对证据与置信度

**依赖**：P0。

**验收**：

- 权杖九样本能表示“保护结构 → 囚禁结构”
- 权杖十样本能表示“牌面未给出的服务关系/皇宫归属”
- 不再用单个 delta 直接触发心理结论
- 单次样本不能生成稳定 trait、成长环境、创伤来源或临床结论
- 至少使用三种不同转换类型的通用样例通过合同测试，避免围绕单牌案例固化规则

---

## P1 · 独立 Traditional Tarot Layer

**目标**：借鉴 Codex Tarot 插件的任务设计，但不依赖其私有 API。

### 数据层

- 以 A. E. Waite 的 *The Pictorial Key to the Tarot* 公版文本作为 RWS 原始基线
- 整理 Pamela Colman Smith 牌面视觉符号
- 为每张牌补充或关联：
  - `waite_description`
  - `waite_upright`
  - `waite_reversed`
  - `source_refs`
  - `visual_inventory`
  - `canonical_dynamic`
  - `reversal_modes`
  - `suit_rank_structure`
  - `modern_psychological_rendering`

**2026-09-11 进度**：78/78 张牌已生成版本化影子记录，并完成逐牌 Waite 章节链接、来源登记、审核状态与确定性重建脚本；公版转录页已自动导入 78 份描述/正位候选与原书实际存在的 77 份逆位候选。旧数据中的 `jungian_mapping` 和心理量表未进入新知识库。候选文本与原书扫描的逐字段审校、现代解释审校和单张产品图授权确认仍待完成。

### 推理层

- **canonical 模式**：只读取卡牌与正逆位
- **situated 模式**：只读取卡牌、正逆位与用户揭晓的问题
- 两种模式都不读取自由联想或 Pass 1 心理结论
- 逆位优先解释为：
  - `blocked`
  - `internalized`
  - `excessive`
  - `misdirected`
  - `delayed`

**依赖**：P0；可与象征转换模型并行设计，但在 Pass 1 前完成。

**验收**：

- canonical 输出不随用户语料变化
- situated 输出能回答问题，但不改写 canonical baseline
- 所有传统牌义都能回到来源或明确标记为 Liminal 的现代解释
- 在显式激活前，影子知识库不得改变当前 canonical payload 或已验收 Prompt

---

## P1 · Pass 1 问题盲态解读

**目标**：在系统尚未收到用户问题时，完成并直接展示第一层解读。

### 内部步骤

1. 语言与副语言特征提取
2. 象征事实提取
3. 象征转换分类
4. 荣格式心理动力解释
5. 生成过程型心理模式：思考顺序、情绪功能、适应价值、当前代价
6. 读取 canonical traditional reading
7. 生成牌的补偿与暂时的第三意义
8. 冻结结构化结果及摘要哈希
9. 写作并直接展示

### 输出要求

- 约 600–900 个中文字符
- 必须独立成立，不是等待 Pass 2 的内部草稿
- 区分观察、解释、假设和无法判断
- 不推断具体童年经历、创伤来源或临床状态
- 至少形成一个可被原话与象征转换共同支持的过程型心理模式；证据不足时明确不生成
- 心理模式描述本次会话如何运作，不写成“你就是怎样的人”

**依赖**：象征转换模型 + Traditional Tarot canonical 模式。

---

## P1 · 问题揭晓与 Pass 2

**目标**：让 Pass 1 成为反思性脚手架，再把用户揭晓的问题带入共时性整合。

### 问题揭晓

- Pass 1 展示后才请求用户输入具体问题
- 引导语说明：问题可以变得更清楚或发生变化，不需要与 Pass 1 一致
- 保存：
  - `user_question`
  - `question_shift`
  - 可选的变化说明

`question_shift` 枚举：

- `unchanged`
- `clarified`
- `shifted_focus`
- `different_question`

### Pass 2

- 输入：冻结的 Pass 1 + situated traditional reading + 用户问题
- 不得覆盖、偷改或完整复述 Pass 1
- 对每个高置信度 Pass 1 模式记录 `integrated`、`held` 或 `not_relevant` 及理由
- 解释冻结模式如何在当前问题中被激活、为何形成困难，以及牌如何补偿该模式
- 后台输出盲态交点、问题变化、继承映射、共时性整合、最终第三意义与方向回答
- 用户侧只输出一篇完整综合解读和一个带走的问题
- 通用领域建议只有在能追溯到冻结模式、用户问题或 situated traditional reading 时才能进入主结论

**初始目标长度**：约 600–900 个中文字符；最终以解释链完整度和人工阅读体验校准。

**依赖**：Pass 1 + Traditional Tarot situated 模式。

---

## P2 · 双阶段写作与产品呈现

**目标**：让证据、荣格式深度与传统塔罗完成感形成递进体验。

### 需要实现

- Pass 1 与 Pass 2 分别生成
- 写作层只能组织已有判断，不能新增心理结论
- Pass 1 结束后提供清晰的“现在揭晓问题”台阶
- 移动端支持渐进披露
- 不默认进入无限聊天

**依赖**：全部 P1 引擎阶段。

---

## P2 · 评测 Harness

**目标**：避免只用“觉得准”评价引擎。

### 必测指标

- Pass 1 独立质量
- 证据支持度
- 用户盲配准确率
- 同语料换牌
- 同牌换语料
- 问题变化分布
- 被诱导强行贴合的主观反馈
- Pass 2 增量价值
- 心理模式辨识度
- Pass 1 → Pass 2 继承度
- 解释链完整度
- 越界心理推断率
- 数日后意义评分

**依赖**：至少一个完整的 P1 纵向流程。

**防过拟合要求**：

- 单次人工试读只作为探索性样本，不直接成为固定规则
- 回归样本至少覆盖不同牌、不同转换类型和不同问题领域
- 人工测试结论只提交匿名化的设计原则与聚合评分，不提交参与者原始语音、问题或可识别材料

---

## P3 · 产品化工程

本轮引擎修正不立即包含：

- PWA 页面与视觉设计
- 实时录音和 ASR 接入
- 用户账户与日志
- GDPR 同意、导出和删除流程
- 危机资源服务
- 纵向 trait/state 展示
- 支付功能

这些工作在引擎方法通过 P2 验证后进入。

---

## 实施顺序

```text
Phase 0：契约修复与基线冻结
    ↓
Phase 1：象征转换 Schema
    ↓
Phase 2：Traditional Tarot Layer
    ↓
Phase 3：Pass 1 问题盲态并直接展示
    ↓
Phase 4：问题揭晓 + Pass 2
    ↓
Phase 5：双阶段写作/展示
    ↓
Phase 6：评测 Harness
```

## 当前状态

- [x] Phase 0（2026-07-28 完成；6 项合同测试通过）
- [x] Phase 1 人工测试前 alpha 契约（2026-08-17：Schema、证据链、过程型心理模式与边界校验）
- [x] Phase 2 人工测试前 provisional 传统层（2026-09-11：78 张隔离接口；78 份描述/正位及 77 份原书逆位候选已导入，逐牌人工审校未完成）
- [x] Phase 3 人工测试前 Pass 1 纵向切片（2026-08-17：四阶段 Prompt、组装、校验与冻结）
- [x] Phase 4 人工测试前 alpha（2026-09-10：问题揭晓、继承映射、现实证据、情境补偿、方向回答与连续写作契约）
- [x] 并行影子基础层（2026-09-11：牌库自动导入完成；分析方法来源表、证据图、争议边界与确认性评测规则完成；七份 Prompt 保持哈希锁定）
- [x] 真实模型适配层（2026-09-11：DeepSeek JSON API、运行时契约封套、逐阶段校验、安全回执及合成七阶段全链路通过）
- [ ] Phase 5
- [ ] Phase 6

## 2026-09-10 Phase 4 人工测试闸门

- Pass 2 请求必须携带经校验且未篡改的冻结 Pass 1，并保留其 SHA-256
- situated traditional reading 只读取 canonical reading 与揭晓问题，不读取 Pass 1 或自由联想
- 每个冻结的 Pass 1 心理模式必须显式标记 `integrated`、`held` 或 `not_relevant`
- 现实支持、限制、反证和“已经采取的行动”只能逐字引用用户揭晓材料；不存在时允许为空，禁止为了鼓励用户而编造
- 最终方向回答必须同时包含不确定性边界，并能回溯到冻结模式或现实证据
- 实践转译可为行动、反思或不生成；不能给每个问题强塞行动建议
- 用户侧只呈现一篇完整解读和一个带走的问题；写作阶段不得新增分析或替换问题
- 当前完整回归以仓库最新测试结果为准；真实模型适配器已接入，下一步直接使用新语料运行完整双阶段真人验收

## Phase 0 完成记录

- `analysis_schema.json` 已清理为单一、合法的 v1.1 JSON
- canonical v1 明确为“三部分分析输出直接面向用户；writing layer deferred”
- schema、analysis prompt 与卡牌结构说明的阈值均为 `delta >= 0.3`
- v1 schema、prompts 与三份 trial reading 的 SHA-256 已写入 baseline manifest
- 没有移动或改写任何 trial reading
- `python3 -m unittest discover -s engine/tests -v`：6 项测试全部通过

## 2026-08-17 Phase 3 人工测试闸门

- v1 文件继续作为冻结基线；v2 位于 `engine/v2/`
- Pass 1 证据阶段看不到传统牌义、旧版主题分数或牌面预设心理标签
- 过程阶段看不到用户问题或传统牌义，只能形成 `session_pattern`
- 补偿阶段才读取 canonical traditional reading，且看不到现实问题
- 写作阶段不得新增分析；最终输出经合同校验后生成稳定哈希
- 回归覆盖权杖九的功能反转、权杖十的关系添加、太阳的情绪改写及弱信号不强行成模
- 下一步需使用不同牌和不同问题进行真人 Pass 1 验证；通过后进入 Phase 4
