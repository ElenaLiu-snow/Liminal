# Liminal · 技术修订清单与优先级

> 日期：2026-07-28
> 目的：把本轮产品讨论转成可执行的技术工作，并明确依赖、优先级和验收条件。
> 原则：每个阶段单独实现、测试和验收；未通过当前阶段前，不提前叠加下一阶段。

---

## 优先级定义

| 优先级 | 含义 |
|---|---|
| **P0 · 阻断** | 当前契约不合法或相互矛盾，不修复就不能安全进入新架构 |
| **P1 · 核心** | 直接实现 Liminal 的差异化机制 |
| **P2 · 质量** | 决定解读是否可靠、可解释、可评测 |
| **P3 · 产品化** | 将已验证的引擎包装成完整用户体验 |

---

## P0 · 契约修复与 v1 基线冻结

**目标**：先让当前 v1 成为合法、可运行、可回归的基线。

### P0-1 修复 `analysis_schema.json`

- 删除第 165 行开始误拼接的旧版 schema
- 保留前半段 v1.1 三部分输出契约
- 确认文件可被标准 JSON parser 读取

**验收**：

- `python -m json.tool engine/analysis_schema.json` 通过
- schema 顶层只包含 `version`、`description`、`input_schema`、`analysis_layer_output`、`pipeline_notes`

### P0-2 收口 v1 架构口径

- canonical v1 口径：分析层三部分输出直接面向用户
- writing layer 是历史实验文件，v1.1 中处于 deferred 状态
- 删除的旧 schema 代表已废弃的“分析层不展示、只供 writing layer 使用”架构

**验收**：

- baseline 文档明确记录上述口径
- 合同测试检查 `pipeline_notes.mvp_output`

### P0-3 统一 delta 阈值

- canonical v1 阈值统一为 `delta >= 0.3`
- 主题空间分数仍只作为 v1 启发式辅助信号
- v2 不把该阈值当作心理测量结果

**验收**：

- schema、analysis prompt 与数据结构说明均使用 `>= 0.3`

### P0-4 冻结回归基线

- 记录 v1 schema、prompts 和三份 trial reading 的文件哈希
- 明确这些文件是后续比较基线，不代表 v2 目标质量
- 不移动、不覆盖现有用户试读文件

**验收**：

- `engine/baselines/v1/manifest.json` 存在并可解析
- manifest 中记录所有基线文件和 SHA-256

### P0-5 增加最小合同测试

- 验证 schema 合法且版本为 1.1
- 验证三部分输出结构存在
- 验证 78 张牌数据均为合法 JSON
- 验证牌数为 22 + 14 × 4
- 验证时间取向和关系方向的数值和

**验收**：

- 标准库 `unittest` 全部通过

---

## P1 · 象征转换关系模型

**目标**：把核心技术对象从“用户偏离标准牌义多少”升级为“用户如何重新组织象征材料”。

### 需要实现

- `symbolic_observations`
  - `noticed`
  - `omitted`
  - `added`
- `symbolic_transformations`
  - `convergence`
  - `omission`
  - `inversion`
  - `addition`
  - `emotional_recoding`
  - `agency_shift`
  - `identification_shift`
  - `temporal_shift`
- 每条转换包含：
  - 用户原话证据
  - 牌面/传统象征证据
  - 首选解释
  - 替代解释
  - 反对证据
  - 置信度

**依赖**：P0。

**验收**：

- 权杖九样本能表示“保护结构 → 囚禁结构”
- 权杖十样本能表示“牌面未给出的服务关系/皇宫归属”
- 不再用单个 delta 直接触发心理结论

---

## P1 · 独立 Traditional Tarot Layer

**目标**：借鉴 Codex Tarot 插件的任务设计，但不依赖其私有 API。

### 数据层

- 以 A. E. Waite 的 *The Pictorial Key to the Tarot* 公版文本作为 RWS 原始基线
- 整理 Pamela Colman Smith 牌面视觉符号
- 为每张牌补充或关联：
  - `waite_description`
  - `waite_upright`
  - `waite_reversed`
  - `source_refs`
  - `visual_inventory`
  - `canonical_dynamic`
  - `reversal_modes`
  - `suit_rank_structure`
  - `modern_psychological_rendering`

### 推理层

- **canonical 模式**：只读取卡牌与正逆位
- **situated 模式**：只读取卡牌、正逆位与用户揭晓的问题
- 两种模式都不读取自由联想或 Pass 1 心理结论
- 逆位优先解释为：
  - `blocked`
  - `internalized`
  - `excessive`
  - `misdirected`
  - `delayed`

**依赖**：P0；可与象征转换模型并行设计，但在 Pass 1 前完成。

**验收**：

- canonical 输出不随用户语料变化
- situated 输出能回答问题，但不改写 canonical baseline
- 所有传统牌义都能回到来源或明确标记为 Liminal 的现代解释

---

## P1 · Pass 1 问题盲态解读

**目标**：在系统尚未收到用户问题时，完成并直接展示第一层解读。

### 内部步骤

1. 语言与副语言特征提取
2. 象征事实提取
3. 象征转换分类
4. 荣格式心理动力解释
5. 读取 canonical traditional reading
6. 生成牌的补偿与暂时的第三意义
7. 冻结结构化结果
8. 写作并直接展示

### 输出要求

- 约 600–900 个中文字符
- 必须独立成立，不是等待 Pass 2 的内部草稿
- 区分观察、解释、假设和无法判断
- 不推断具体童年经历、创伤来源或临床状态

**依赖**：象征转换模型 + Traditional Tarot canonical 模式。

---

## P1 · 问题揭晓与 Pass 2

**目标**：让 Pass 1 成为反思性脚手架，再把用户揭晓的问题带入共时性整合。

### 问题揭晓

- Pass 1 展示后才请求用户输入具体问题
- 引导语说明：问题可以变得更清楚或发生变化，不需要与 Pass 1 一致
- 保存：
  - `user_question`
  - `question_shift`
  - 可选的变化说明

`question_shift` 枚举：

- `unchanged`
- `clarified`
- `shifted_focus`
- `different_question`

### Pass 2

- 输入：冻结的 Pass 1 + situated traditional reading + 用户问题
- 不得覆盖、偷改或完整复述 Pass 1
- 输出：
  - 盲态发现与具体问题的交点
  - 问题变化及其意义
  - 共时性整合
  - 最终第三意义
  - 有边界的方向回答
  - 带走的问题

**目标长度**：约 500–800 个中文字符。

**依赖**：Pass 1 + Traditional Tarot situated 模式。

---

## P2 · 双阶段写作与产品呈现

**目标**：让证据、荣格式深度与传统塔罗完成感形成递进体验。

### 需要实现

- Pass 1 与 Pass 2 分别生成
- 写作层只能组织已有判断，不能新增心理结论
- Pass 1 结束后提供清晰的“现在揭晓问题”台阶
- 移动端支持渐进披露
- 不默认进入无限聊天

**依赖**：全部 P1 引擎阶段。

---

## P2 · 评测 Harness

**目标**：避免只用“觉得准”评价引擎。

### 必测指标

- Pass 1 独立质量
- 证据支持度
- 用户盲配准确率
- 同语料换牌
- 同牌换语料
- 问题变化分布
- 被诱导强行贴合的主观反馈
- Pass 2 增量价值
- 越界心理推断率
- 数日后意义评分

**依赖**：至少一个完整的 P1 纵向流程。

---

## P3 · 产品化工程

本轮引擎修正不立即包含：

- PWA 页面与视觉设计
- 实时录音和 ASR 接入
- 用户账户与日志
- GDPR 同意、导出和删除流程
- 危机资源服务
- 纵向 trait/state 展示
- 支付功能

这些工作在引擎方法通过 P2 验证后进入。

---

## 实施顺序

```text
Phase 0：契约修复与基线冻结
    ↓
Phase 1：象征转换 Schema
    ↓
Phase 2：Traditional Tarot Layer
    ↓
Phase 3：Pass 1 问题盲态并直接展示
    ↓
Phase 4：问题揭晓 + Pass 2
    ↓
Phase 5：双阶段写作/展示
    ↓
Phase 6：评测 Harness
```

## 当前状态

- [x] Phase 0（2026-07-28 完成；6 项合同测试通过）
- [ ] Phase 1
- [ ] Phase 2
- [ ] Phase 3
- [ ] Phase 4
- [ ] Phase 5
- [ ] Phase 6

## Phase 0 完成记录

- `analysis_schema.json` 已清理为单一、合法的 v1.1 JSON
- canonical v1 明确为“三部分分析输出直接面向用户；writing layer deferred”
- schema、analysis prompt 与卡牌结构说明的阈值均为 `delta >= 0.3`
- v1 schema、prompts 与三份 trial reading 的 SHA-256 已写入 baseline manifest
- 没有移动或改写任何 trial reading
- `python3 -m unittest discover -s engine/tests -v`：6 项测试全部通过

下一步是 Phase 1：先设计并评审象征转换 schema，再修改分析 prompt；不在 Phase 0 内提前改动。
